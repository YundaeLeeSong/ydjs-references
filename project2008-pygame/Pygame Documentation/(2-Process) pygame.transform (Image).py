# pygame module to transform surfaces
def rotate(surface_object, angle): # (surface_object: Surface, angle: float, return: Surface)
    # Rotate an image in counterclockwise rotation
    # The angle argument represents angles in degrees (Negative angle amounts will rotate clockwise)
    # Unless rotating by 90 degree increments, the image will be larger to hold the new size.
    # If the image has pixel alphas, the padded area will be transparent.
    return surface_object
